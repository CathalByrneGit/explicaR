% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{targets_network}
\alias{targets_network}
\title{Build a parse_result-compatible graph from a targets pipeline}
\usage{
targets_network(
  project_dir = ".",
  store = file.path(project_dir, "_targets"),
  script = file.path(project_dir, "_targets.R"),
  outdated = FALSE
)
}
\arguments{
\item{project_dir}{Path to the project directory.}

\item{store}{Path to the targets store.}

\item{script}{Path to \verb{_targets.R}. Defaults to \verb{_targets.R} inside
\code{project_dir}.}

\item{outdated}{Logical; whether to compute outdated status for each target
(shown in node tooltips). Adds a small amount of computation. Default
\code{FALSE}.}
}
\value{
A list with:
\itemize{
\item \code{nodes}: tibble in \code{explicar_parse()} node format, plus \code{status} and
\code{seconds} columns populated when available
\item \code{edges}: tibble with \code{from}, \code{to}, \code{type}
\item \code{manifest}: raw \code{tar_manifest()} output
}
}
\description{
Uses \code{tar_network()} for the exact dependency graph and merges target
descriptions from \code{tar_manifest()} as node labels — both are free when
targets is available, replacing the slower CodeDepends-based parsing.
}
\details{
Node labels come from the \code{description} field set in \code{tar_target(..., description = "...")} (targets >= 1.3.0). When no description is set the
target name is used as a fallback.
}
