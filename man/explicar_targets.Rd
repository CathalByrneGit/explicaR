% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{explicar_targets}
\alias{explicar_targets}
\title{Read intermediate objects from a targets cache}
\usage{
explicar_targets(
  project_dir = ".",
  names = NULL,
  store = file.path(project_dir, "_targets")
)
}
\arguments{
\item{project_dir}{Path to the project directory (must contain a
\verb{_targets/} subdirectory or a \verb{_targets.yaml} config file).}

\item{names}{Optional character vector of target names to read. If \code{NULL}
(default) all available targets are read.}

\item{store}{Path to the targets store. Defaults to
\code{file.path(project_dir, "_targets")}.}
}
\value{
A named list of cached R objects (one element per target name).
Returns an empty list if targets is unavailable or the cache does not
exist.
}
\description{
Soft-depends on the \code{{targets}} package. When a project uses targets,
this module reads cached intermediate objects and returns them as a named
list that can be passed to \code{\link[=attach_shapes]{attach_shapes()}} or the animation layer.
}
\examples{
\dontrun{
cache <- explicar_targets("path/to/project")
names(cache)
}
}
