% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{explicar_index_build}
\alias{explicar_index_build}
\title{Build or incrementally update the persistent explicaR index}
\usage{
explicar_index_build(
  project_dir = ".",
  pattern = "*.R",
  recursive = TRUE,
  embed = FALSE,
  embed_model = "nomic-embed-text",
  ollama_url = "http://localhost:11434",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{project_dir}{Path to the R project directory. Default \code{"."}.}

\item{pattern}{Glob pattern for R scripts. Default \code{"*.R"}.}

\item{recursive}{Recurse into sub-directories. Default \code{TRUE}.}

\item{embed}{Generate text embeddings for semantic retrieval.
Requires Ollama to be running and the \code{httr2} package.  Default \code{FALSE}.}

\item{embed_model}{Ollama embedding model. Default \code{"nomic-embed-text"}.}

\item{ollama_url}{Ollama API base URL. Default \code{"http://localhost:11434"}.}

\item{force}{Re-parse all files even if unchanged. Default \code{FALSE}.}

\item{quiet}{Suppress progress messages. Default \code{FALSE}.}
}
\value{
Invisibly, the path to the index database file.
}
\description{
Parses R scripts in \code{project_dir} and stores nodes, edges, and verbs in a
DuckDB database at \code{.explicar/index.duckdb}.  On subsequent calls only files
whose modification time has changed are re-parsed, so the operation is fast
after the initial build.
}
\examples{
\dontrun{
# Build the index for the current project
explicar_index_build()

# Force a full rebuild
explicar_index_build(force = TRUE)

# Also embed nodes for semantic search (requires Ollama)
explicar_index_build(embed = TRUE, embed_model = "nomic-embed-text")
}
}
