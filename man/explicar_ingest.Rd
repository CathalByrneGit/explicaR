% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{explicar_ingest}
\alias{explicar_ingest}
\title{Ingest project docs into the ragnar RAG store}
\usage{
explicar_ingest(
  project_dir = ".",
  db_path = NULL,
  include = c("wiki", "source", "readme", "vignettes"),
  embed = FALSE,
  embed_model = "nomic-embed-text",
  ollama_url = "http://localhost:11434",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{project_dir}{Project directory. Default \code{"."}.}

\item{db_path}{Path to the store. Defaults to \code{.explicar/explicar.duckdb}.}

\item{include}{Sources to ingest. Any subset of
\code{c("wiki", "source", "readme", "vignettes")}.}

\item{embed}{Generate vector embeddings (requires Ollama). Default \code{FALSE}.}

\item{embed_model}{Ollama embedding model.}

\item{ollama_url}{Ollama API base URL.}

\item{force}{Re-ingest even if chunks already exist. Default \code{FALSE}.}

\item{quiet}{Suppress progress messages. Default \code{FALSE}.}
}
\value{
Invisibly, the total number of chunks ingested.
}
\description{
Reads wiki pages (from the \code{wiki} table built by \code{\link[=explicar_wiki_build]{explicar_wiki_build()}}),
roxygen source comments, and README into the ragnar-backed store so that
\code{\link[=explicar_doc_retrieve]{explicar_doc_retrieve()}} and \code{\link[=deep_research]{deep_research()}} can query them.
}
\details{
The store lives at \code{.explicar/explicar.duckdb} alongside the code-graph
tables.
}
\examples{
\dontrun{
# Build wiki first, then ingest everything
explicar_wiki_build("path/to/project")
explicar_ingest("path/to/project")

# With vector embeddings for semantic search (Ollama required)
explicar_ingest("path/to/project", embed = TRUE)
}
}
