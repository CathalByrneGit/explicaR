% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{explicar_parse}
\alias{explicar_parse}
\title{Parse scripts to produce a pipeline edge list}
\usage{
explicar_parse(
  project_dir = ".",
  pattern = NULL,
  recursive = FALSE,
  backend = c("auto", "treesitter", "r"),
  languages = "r"
)
}
\arguments{
\item{project_dir}{Path to the project directory containing scripts.}

\item{pattern}{Regex pattern to match script files. When \code{NULL} (default)
it is derived from \code{languages}: \code{"\\\\.R$"} for R-only, \code{"\\\\.R$|\\\\.py$"}
for R + Python.}

\item{recursive}{Logical; whether to search sub-directories (default \code{FALSE}).}

\item{backend}{One of \code{"auto"} (default), \code{"treesitter"}, or \code{"r"}. Only
affects R files; Python files always use the best available Python backend.}

\item{languages}{Character vector of languages to parse. Any subset of
\code{c("r", "python")}. Default \code{"r"}.}
}
\value{
A list with:
\itemize{
\item \code{nodes}: tibble of nodes (name, type, file, line, label, shape_info)
\item \code{edges}: tibble of edges (from, to, type)
\item \code{verbs}: tibble of verb call records for the animation layer
}
}
\description{
Dispatches to the best available parse backend. R files are analysed in
priority order: treesitter → base-R \code{getParseData()}. Python files are
parsed when \code{languages} includes \code{"python"} (requires \code{treesitter.python}
or falls back to a pure-regex approach).
}
\examples{
\dontrun{
# R only (default)
result <- explicar_parse("path/to/project")

# Mixed R + Python project
result <- explicar_parse("path/to/project", languages = c("r", "python"))
result$nodes
result$edges
}
}
