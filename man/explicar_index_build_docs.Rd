% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-docs.R
\name{explicar_index_build_docs}
\alias{explicar_index_build_docs}
\title{Build a local documentation index from package sources}
\usage{
explicar_index_build_docs(
  project_dir = ".",
  include = c("man", "source", "readme", "vignettes"),
  embed = FALSE,
  embed_model = "nomic-embed-text",
  ollama_url = "http://localhost:11434",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{project_dir}{Path to the R project directory.  Default \code{"."}.}

\item{include}{Character vector of source types to include.  Any subset of
\code{c("man", "source", "readme", "vignettes")}.}

\item{embed}{Embed each chunk via Ollama for vector retrieval.}

\item{embed_model}{Ollama embedding model.  Default \code{"nomic-embed-text"}.}

\item{ollama_url}{Ollama API base URL.}

\item{force}{Re-build even if docs are already stored.}

\item{quiet}{Suppress progress messages.}
}
\value{
Invisibly, the number of chunks stored.
}
\description{
Extracts documentation from the R package's own source files — roxygen2
comments in \verb{R/*.R}, \verb{man/*.Rd} (if generated), \code{README.md}, and
\verb{vignettes/} — and stores the result as searchable chunks in the
\code{.explicar/index.duckdb} \code{docs} table.  Everything stays on disk: no
network calls, no external services, safe for private repositories.
}
\details{
Sources are processed in priority order:
\enumerate{
\item \verb{man/*.Rd} files (most structured; run \code{devtools::document()} to
generate them from roxygen2 comments).
\item Roxygen2 \verb{#'} comment blocks extracted directly from \verb{R/*.R} source
(fallback when \verb{man/} is empty or for unexported internals).
\item \code{README.md} / \code{README.Rmd}.
\item Any \verb{*.Rmd} / \verb{*.qmd} / \verb{*.md} files under \verb{vignettes/}.
}
}
\examples{
\dontrun{
# Full local doc index (no network, works for private repos)
explicar_index_build_docs()

# Only README + vignettes
explicar_index_build_docs(include = c("readme", "vignettes"))

# Build + embed for semantic search
explicar_index_build_docs(embed = TRUE, embed_model = "nomic-embed-text")

# After building, retrieve relevant docs
explicar_index_retrieve("how does verb animation work")
}
}
