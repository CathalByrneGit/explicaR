% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_explicar_db.R
\name{view_explicar_db}
\alias{view_explicar_db}
\title{Launch the Tier-3 interactive analytics server}
\usage{
view_explicar_db(
  project_dir = ".",
  db_path = NULL,
  llm_chat = NULL,
  top_k = 10L,
  port = 8080L,
  host = "127.0.0.1",
  open = TRUE
)
}
\arguments{
\item{project_dir}{Project to serve. Default \code{"."}.}

\item{db_path}{Explicar DuckDB path. Defaults to \code{.explicar/explicar.duckdb}.}

\item{llm_chat}{An \code{ellmer::Chat} object.  When \code{NULL}, the chat panel is
disabled.  Retrieval from the ragnar store is wired automatically when
the store exists.}

\item{top_k}{Chunks retrieved per query. Default \code{10L}.}

\item{port}{HTTP port. Default \code{8080L}.}

\item{host}{Host to bind. Default \code{"127.0.0.1"} (localhost only).}

\item{open}{Open the browser automatically. Default \code{TRUE}.}
}
\value{
Invisibly, the httpuv server object.  Returned only when the
blocking loop is exited (e.g. via timeout or Ctrl+C in non-interactive
use).
}
\description{
Starts a local HTTP server serving the full analytics dashboard at
\verb{http://host:port/}.  The dashboard includes:
\itemize{
\item The Mermaid pipeline graph with clickable node detail
\item LLM-generated wiki pages per source file
\item BM25 keyword search over the ragnar RAG store
\item LLM chat panel backed by ragnar retrieval (when \code{llm_chat} is supplied)
}
}
\details{
The server blocks until interrupted (Ctrl+C).  Stop programmatically with
\code{httpuv::stopServer(srv)} on the returned object.
}
\examples{
\dontrun{
library(ellmer)
# With Ollama running locally
view_explicar_db(llm_chat = chat_ollama(model = "llama3.2"))

# Search only (no LLM)
view_explicar_db()
}
}
