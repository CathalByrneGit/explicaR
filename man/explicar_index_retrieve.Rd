% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{explicar_index_retrieve}
\alias{explicar_index_retrieve}
\title{Retrieve nodes from the persistent index}
\usage{
explicar_index_retrieve(
  query,
  project_dir = ".",
  top_k = 10L,
  type = NULL,
  embed_model = "nomic-embed-text",
  ollama_url = "http://localhost:11434"
)
}
\arguments{
\item{query}{Search query string.}

\item{project_dir}{Path to the R project directory. Default \code{"."}.}

\item{top_k}{Maximum number of results. Default \code{10L}.}

\item{type}{Optional node-type filter, e.g. \code{"function"}.}

\item{embed_model}{Ollama model used to embed the query (must match the
model used at index time). Default \code{"nomic-embed-text"}.}

\item{ollama_url}{Ollama API base URL.}
}
\value{
A \link[tibble:tibble]{tibble} of matching nodes, ordered by relevance,
with columns \code{name}, \code{type}, \code{file}, \code{line}, \code{label}, \code{shape_info}, and
optionally \code{similarity} when vector search is used.
}
\description{
Searches the index for nodes matching \code{query}.  If embeddings are available
a combined keyword + cosine-similarity ranking is used; otherwise a simple
case-insensitive keyword search over \code{name} and \code{label} is performed.
}
\examples{
\dontrun{
explicar_index_retrieve("clean survey data")
explicar_index_retrieve("pivot", type = "function", top_k = 5)
}
}
