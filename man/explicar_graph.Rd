% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{explicar_graph}
\alias{explicar_graph}
\title{Build a Mermaid flowchart from a parse result}
\usage{
explicar_graph(
  parse_result,
  direction = c("TD", "LR", "BT", "RL"),
  max_label = 40L
)
}
\arguments{
\item{parse_result}{Output from \code{\link[=explicar_parse]{explicar_parse()}}.}

\item{direction}{One of \code{"TD"} (top-down, default), \code{"LR"} (left-right),
\code{"BT"} (bottom-top), or \code{"RL"} (right-left).}

\item{max_label}{Integer; truncate long node labels to this many characters.
Default \code{40L}.}
}
\value{
A single character string containing Mermaid flowchart syntax.
}
\description{
Returns a Mermaid \code{flowchart} diagram as a plain character string.
Embed it in HTML with \code{\link[=generate_viewer]{generate_viewer()}} or paste into any Mermaid
renderer (GitHub, Quarto, Obsidian, etc.).
}
\examples{
\dontrun{
pr  <- explicar_parse("path/to/project")
cat(explicar_graph(pr))
}
}
