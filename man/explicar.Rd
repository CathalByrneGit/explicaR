% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{explicar}
\alias{explicar}
\title{Generate the explicaR pipeline viewer}
\usage{
explicar(
  project_dir = ".",
  output_file = NULL,
  output_dir = NULL,
  title = NULL,
  pkg_name = NULL,
  languages = "r",
  snapshots = NULL,
  llm = FALSE,
  llm_chat = NULL,
  enrich = FALSE,
  llm_model = "qwen2.5-coder:3b",
  embed = NULL,
  embed_model = "nomic-embed-text",
  ingest = TRUE,
  direction = "TD",
  open = TRUE,
  db_extensions = FALSE,
  git_pat = NULL,
  update = TRUE,
  project_path = NULL
)
}
\arguments{
\item{project_dir}{Path to the project — a local directory \strong{or} a remote
URL (GitHub, GitLab, Bitbucket).  Remote repos are cloned to
\verb{~/.explicar/repos/<host>/<owner>/<repo>/} and pulled on subsequent calls.
Alias \code{project_path} also accepted.}

\item{output_file}{Output HTML path.  Defaults to
\verb{<project_dir>/explicar_viewer.html}.}

\item{output_dir}{Directory for all output files (overrides \code{output_file} base
directory when set).}

\item{title}{Page title / \code{pkg_name}. Defaults to
\code{"explicaR — <project name>"}.}

\item{pkg_name}{Alias for \code{title}.}

\item{languages}{Languages to parse: \code{c("r")} (default), \code{c("r","python")}.}

\item{snapshots}{Optional named list of intermediate dataframes (from
\code{\link[=with_pipeline_trace]{with_pipeline_trace()}} or \code{\link[=explicar_targets]{explicar_targets()}}).}

\item{llm}{Logical; generate LLM wiki pages (requires ellmer + Ollama or
an explicit \code{llm_chat}). Default \code{FALSE}.}

\item{llm_chat}{An \code{ellmer::Chat} object.  When supplied and \code{llm = TRUE}
(or when \code{llm_chat} is non-NULL), wiki pages are generated using it.}

\item{enrich}{Logical; enrich undocumented function-node labels via Ollama
(uses the lightweight httr2 path, not ellmer). Default \code{FALSE}.}

\item{llm_model}{Ollama model for \code{enrich = TRUE}.}

\item{embed}{Logical or \code{NULL}; embed wiki chunks into the ragnar store.
\code{NULL} (default) = embed only when Ollama is available.}

\item{embed_model}{Ollama embedding model.}

\item{ingest}{Logical; ingest wiki + docs into ragnar after wiki build.
Ignored when \code{llm = FALSE}. Default \code{TRUE}.}

\item{direction}{Mermaid graph direction: \code{"TD"} (default), \code{"LR"}, etc.}

\item{open}{Open the viewer in the browser. Default \code{TRUE}.}

\item{db_extensions}{Logical; reserved for future DuckDB extension support.
Default \code{FALSE}.}

\item{git_pat}{Personal access token for private repositories.  When \code{NULL}
(default), reads \code{GITHUB_PAT} / \code{GITLAB_TOKEN} / \code{BITBUCKET_TOKEN} from
the environment.}

\item{update}{Logical; run \verb{git pull} if a cached clone already exists.
Set \code{FALSE} to use the cached version without fetching.  Default \code{TRUE}.}

\item{project_path}{Deprecated alias for \code{project_dir}.}
}
\value{
Invisibly, the path to the generated HTML file.
}
\description{
Main entry point.  Parses the project, optionally builds LLM wiki pages and
ingests them into the ragnar store, then writes a self-contained HTML viewer.
}
\examples{
\dontrun{
# Minimal — just the graph
explicar("path/to/project")

# With LLM wiki (Ollama running locally)
explicar("path/to/project", llm = TRUE)

# Bring-your-own chat (any ellmer provider)
library(ellmer)
chat <- chat_openai(model = "gpt-4o-mini")
explicar("path/to/project", llm_chat = chat)

# Mixed R + Python project
explicar("path/to/project", languages = c("r", "python"))

# Remote public repo
explicar("https://github.com/tidyverse/dplyr", llm = TRUE)

# Remote private repo (PAT from environment or explicit)
explicar("https://github.com/org/private-repo",
         git_pat = Sys.getenv("GITHUB_PAT"))
}
}
