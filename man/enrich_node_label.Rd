% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{enrich_node_label}
\alias{enrich_node_label}
\title{Optional LLM label enrichment via Ollama}
\usage{
enrich_node_label(
  fn_name_or_body,
  model = "qwen2.5-coder:3b",
  ollama_url = "http://localhost:11434",
  max_words = 10L,
  timeout_s = 15L
)
}
\arguments{
\item{fn_name_or_body}{Either a function name (character) to look up in the
current session, or the full body text of the function as a string.}

\item{model}{Ollama model name. Default \code{"qwen2.5-coder:3b"}.}

\item{ollama_url}{Base URL for the Ollama API. Default
\code{"http://localhost:11434"}.}

\item{max_words}{Maximum words in the returned description. Default \code{10L}.}

\item{timeout_s}{Request timeout in seconds. Default \code{15L}.}
}
\value{
A character string with the LLM-generated description, or \code{NA} if
enrichment fails or Ollama is unavailable.
}
\description{
Calls a locally running \href{https://ollama.com}{Ollama} instance to generate
plain-English descriptions for undocumented function nodes.
}
\details{
This is a \strong{soft dependency} — the package works without it. LLM enrichment
is used only as a \strong{last resort}, after checking for roxygen documentation
and inline comments. See the priority rule in the package documentation.
}
\section{Priority rule}{

\enumerate{
\item Use roxygen \verb{@title}/\verb{@description} verbatim if present — skip LLM.
\item Use inline comments above the function as context.
\item Only call the LLM for completely undocumented functions.
}
}

\examples{
\dontrun{
enrich_node_label("clean_survey")
enrich_node_label("function(df) df |> filter(!is.na(id)) |> distinct()")
}
}
