% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate.R
\name{explicar_animate}
\alias{explicar_animate}
\title{Build datamations animations for pipeline verb nodes}
\usage{
explicar_animate(parse_result, snapshots = NULL, max_rows = 50L, verbs = NULL)
}
\arguments{
\item{parse_result}{Output from \code{\link[=explicar_parse]{explicar_parse()}}.}

\item{snapshots}{Named list of real intermediate dataframes (from
\code{\link[=explicar_targets]{explicar_targets()}} or \code{\link[=with_pipeline_trace]{with_pipeline_trace()}}). When \code{NULL}, illustrative
mode is used for all verb nodes.}

\item{max_rows}{Maximum rows to show in animations (default 50). Larger
dataframes are sampled down for clarity.}

\item{verbs}{Character vector of verb names to animate. \code{NULL} (default)
animates all supported verbs found in the parse result.}
}
\value{
A named list where each element is a list with:
\itemize{
\item \code{descriptor}: the verb descriptor (from \code{\link[=verb_descriptor]{verb_descriptor()}})
\item \code{widget}: a datamations htmlwidget (if datamations is available) or
a plain HTML fallback
\item \code{json}: the Vega-Lite animation spec as a character string
}
}
\description{
\code{explicar_animate()} takes the output of \code{\link[=explicar_parse]{explicar_parse()}} and a snapshot
list of intermediate dataframes, and returns a named list of animation
objects — one per verb record in the parse result.
}
\details{
Two modes are supported:
\itemize{
\item \strong{Illustrative} (default): generates a small synthetic dataframe that
matches column names/types of the real data but uses toy values. Safe and
fast.
\item \strong{Real}: uses actual intermediate snapshots from the trace or targets
cache. More informative but requires execution.
}
}
\examples{
\dontrun{
pr  <- explicar_parse("path/to/project")
snaps <- with_pipeline_trace("path/to/project/pipeline.R")
anim <- explicar_animate(pr, snapshots = snaps)
anim[["filter@clean.R:23"]]$widget
}
}
