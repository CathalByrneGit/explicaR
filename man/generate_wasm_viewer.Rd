% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_wasm.R
\name{generate_wasm_viewer}
\alias{generate_wasm_viewer}
\title{Generate a Tier-2 WASM-powered pipeline viewer}
\usage{
generate_wasm_viewer(
  parse_result,
  title = NULL,
  output_file = "explicar_wasm.html",
  direction = "TD",
  open = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{parse_result}{Output from \code{\link[=explicar_parse]{explicar_parse()}}.}

\item{title}{Page title. Defaults to \code{"explicaR — WASM Viewer"}.}

\item{output_file}{Path for the output HTML file.}

\item{direction}{Mermaid graph direction: \code{"TD"} (default), \code{"LR"}, etc.}

\item{open}{Logical; open the file in the browser when done. Default \code{TRUE}.}

\item{quiet}{Suppress progress messages. Default \code{FALSE}.}
}
\value{
Invisibly, the path to the generated HTML file.
}
\description{
Like \code{\link[=generate_viewer]{generate_viewer()}} but embeds an in-browser DuckDB-WASM engine that
lets users run arbitrary SQL queries (e.g.
\verb{SELECT * FROM nodes WHERE type = 'function'}) against the code graph
without any server. Requires an internet connection for DuckDB-WASM and
Mermaid CDN assets.
}
\details{
The viewer exposes three tables:
\itemize{
\item \strong{nodes} — \verb{name, type, file, line, label, shape_info}
\item \strong{edges} — \verb{from_node, to_node, type}
\item \strong{verbs} — \verb{file, line, fn_name, input_var, output_var, pkg}
}
}
\examples{
\dontrun{
pr <- explicar_parse("path/to/project")
generate_wasm_viewer(pr, output_file = "pipeline_wasm.html")
}
}
