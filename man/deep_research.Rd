% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wiki.R
\name{deep_research}
\alias{deep_research}
\title{Multi-turn iterative research using an ellmer Chat}
\usage{
deep_research(chat, question, max_iterations = 5L)
}
\arguments{
\item{chat}{An \code{ellmer::Chat} object, typically with a retrieval tool
already wired via \code{\link[ragnar:ragnar_register_tool_retrieve]{ragnar::ragnar_register_tool_retrieve()}}.}

\item{question}{The research question.}

\item{max_iterations}{Maximum number of LLM turns (including the initial
planning turn). Default \code{5L}.}
}
\value{
Invisibly, the updated \code{chat} object (full history is preserved).
}
\description{
Runs up to \code{max_iterations} turns of a research conversation.  On each
turn the LLM may call any retrieval tools registered on the chat (e.g.
via \code{ragnar::ragnar_register_tool_retrieve()}).  The loop terminates
early when the response begins with \verb{CONCLUSION:}.
}
\examples{
\dontrun{
library(ellmer)
library(ragnar)
store <- ragnar_store_connect(".explicar/explicar.duckdb")
chat  <- chat_ollama(model = "llama3.2")
ragnar_register_tool_retrieve(chat, store)
deep_research(chat, "How does the parse dispatch layer work?")
cat(chat$last_turn()$text)
}
}
