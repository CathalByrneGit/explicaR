% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-docs.R
\name{explicar_index_generate_wiki}
\alias{explicar_index_generate_wiki}
\title{Generate a wiki from your package using a local LLM}
\usage{
explicar_index_generate_wiki(
  project_dir = ".",
  model = "llama3.2",
  ollama_url = "http://localhost:11434",
  include = c("files", "architecture"),
  max_file_chars = 6000L,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{project_dir}{Path to the R project.  Default \code{"."}.}

\item{model}{Ollama model name to use for generation, e.g.
\code{"llama3.2"}, \code{"mistral"}, \code{"gemma3"}, \code{"phi4"}.}

\item{ollama_url}{Ollama API base URL.  Default \code{"http://localhost:11434"}.}

\item{include}{Character vector.  \code{"files"} generates one wiki page per
\verb{R/*.R} source file.  \code{"architecture"} generates a single cross-file
architecture overview.  Default is both.}

\item{max_file_chars}{Maximum characters of source code sent per file
(prevents very large files from overflowing the model's context).
Default 6000.}

\item{force}{Overwrite existing wiki pages for this model/project.}

\item{quiet}{Suppress progress messages.}
}
\value{
Invisibly, the number of wiki pages stored.
}
\description{
Sends each R source file (and optionally a cross-file architecture prompt)
to a locally-running \href{https://ollama.com}{Ollama} model and asks it to
write a narrative wiki page in markdown.  The pages are stored in the
\code{.explicar/index.duckdb} \code{docs} table alongside any content added by
\code{\link[=explicar_index_build_docs]{explicar_index_build_docs()}}, so \code{\link[=explicar_index_retrieve]{explicar_index_retrieve()}} can search
all of them together.
}
\details{
Nothing leaves your machine.  All inference is done by the Ollama server
you already run locally (same server used for embeddings).
}
\examples{
\dontrun{
# Generate wiki with any locally-available Ollama model
explicar_index_generate_wiki(model = "llama3.2")

# Only the architecture overview page
explicar_index_generate_wiki(model = "mistral", include = "architecture")

# Then retrieve across code + wiki together
explicar_index_retrieve("how does the animation pipeline work")
}
}
