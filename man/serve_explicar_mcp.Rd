% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve_mcp.R
\name{serve_explicar_mcp}
\alias{serve_explicar_mcp}
\title{Start the explicaR MCP stdio server}
\usage{
serve_explicar_mcp(project_dir = ".", db_path = NULL)
}
\arguments{
\item{project_dir}{Project to serve. Default \code{"."}.}

\item{db_path}{Explicar DuckDB path. Defaults to \code{.explicar/explicar.duckdb}.}
}
\value{
Called for its side effect (blocks until stdin closes).
}
\description{
Launches a \href{https://modelcontextprotocol.io}{Model Context Protocol} server
over stdio so Claude Desktop / Claude Code can query the code graph and
wiki pages for this project.
}
\details{
Add to your \code{claude_desktop_config.json}:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "my-project": \{
      "command": "Rscript",
      "args": ["-e", "explicaR::serve_explicar_mcp('path/to/project')"]
    \}
  \}
\}
}\if{html}{\out{</div>}}

The server exposes four tools:
\itemize{
\item \strong{\code{search_code}} — hybrid BM25 / keyword search over the codebase
\item \strong{\code{query_graph}} — run \code{SELECT} SQL against \code{nodes}, \code{edges}, \code{wiki},
\code{verbs} tables
\item \strong{\code{get_wiki}} — retrieve the LLM-generated wiki page for a file
\item \strong{\code{list_files}} — list all source files with function summaries
}
}
\examples{
\dontrun{
# Build the index first, then start the server
explicar_index_build("path/to/project")
serve_explicar_mcp("path/to/project")
}
}
