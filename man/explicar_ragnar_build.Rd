% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-ragnar.R
\name{explicar_ragnar_build}
\alias{explicar_ragnar_build}
\title{Build (or update) the ragnar documentation store}
\usage{
explicar_ragnar_build(
  project_dir = ".",
  include = c("man", "source", "readme", "vignettes"),
  embed = TRUE,
  embed_model = "nomic-embed-text",
  ollama_url = "http://localhost:11434",
  target_size = 1000L,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{project_dir}{Path to the R project directory.  Default \code{"."}.}

\item{include}{Sources to index: any subset of \code{c("man", "source", "readme", "vignettes")}.}

\item{embed}{Logical; generate vector embeddings via Ollama for semantic
(VSS) search.  Requires Ollama running locally.  Default \code{TRUE}.}

\item{embed_model}{Ollama embedding model.  Default \code{"nomic-embed-text"}.}

\item{ollama_url}{Ollama API base URL.}

\item{target_size}{Target chunk size in characters.  Default \code{1000L}.}

\item{force}{Re-index even if the store already contains entries for this
package.  Default \code{FALSE}.}

\item{quiet}{Suppress progress messages.  Default \code{FALSE}.}
}
\value{
Invisibly, the path to the ragnar store file.
}
\description{
Extracts documentation from the package's own sources, chunks it with
\code{ragnar::markdown_chunk()}, and stores the result in a DuckDB-backed
ragnar store at \code{.explicar/docs.ragnar.duckdb}.  Hybrid BM25 + vector-
similarity retrieval is available automatically when Ollama is running.
}
\examples{
\dontrun{
# Build with vector embeddings (Ollama must be running)
explicar_ragnar_build()

# BM25-only (no Ollama needed)
explicar_ragnar_build(embed = FALSE)

# Then retrieve
explicar_doc_retrieve("how does verb animation work")
}
}
