% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{explicar_embed}
\alias{explicar_embed}
\title{Embed code nodes into the ragnar vector store}
\usage{
explicar_embed(
  project_dir = ".",
  db_path = NULL,
  model = "nomic-embed-text",
  ollama_url = "http://localhost:11434",
  include = c("nodes", "wiki"),
  batch_size = 20L,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{project_dir}{Project directory.  Default \code{"."}.}

\item{db_path}{Path to the unified explicaR DuckDB.  Defaults to
\code{.explicar/explicar.duckdb}.}

\item{model}{Ollama embedding model.  Default \code{"nomic-embed-text"}.}

\item{ollama_url}{Ollama base URL.  Default \code{"http://localhost:11434"}.}

\item{include}{What to embed.  Any subset of
\code{c("nodes", "wiki", "source", "readme", "vignettes")}.
\code{"nodes"} embeds code-graph nodes directly; the rest delegate to
\code{\link[=explicar_ingest]{explicar_ingest()}} with \code{embed = TRUE}.}

\item{batch_size}{Nodes per progress update.  Default \code{20L}.}

\item{force}{Re-embed even if already present.  Default \code{FALSE}.}

\item{quiet}{Suppress progress messages.  Default \code{FALSE}.}
}
\value{
Invisibly, the number of node chunks embedded.
}
\description{
Converts parsed code-graph nodes (functions, scripts, variables) into
natural-language text descriptions and inserts them into the ragnar RAG
store at \code{.explicar/explicar.duckdb} so that vector-similarity search is
available via \code{\link[=explicar_semantic_retrieve]{explicar_semantic_retrieve()}} and the MCP \code{search_code} tool.
}
\details{
Run \code{\link[=explicar_index_build]{explicar_index_build()}} first to populate the nodes table, then call
this once (or whenever the project changes significantly).
}
\examples{
\dontrun{
# Build index + wiki first, then embed
explicar_index_build()
explicar_wiki_build()
explicar_embed()

# Nodes only (fastest)
explicar_embed(include = "nodes")

# Then search semantically
explicar_semantic_retrieve("clean survey data")
}
}
